The user prefixed `fix:` — they want to record a correction so Claude doesn't misbehave the same way again.

Append a single-line imperative bullet (strip filler like "please", "I want") under `## Corrections` in `{dst}`. The file and the `CLAUDE.md` @import are already bootstrapped. Skip if already covered; replace if it contradicts an existing bullet (latest wins).

Confirm in one short line. No other action.
