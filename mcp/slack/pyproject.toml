[project]
name = "slack-mcp"
version = "0.1.0"
description = "MCP server for sending Slack messages and files as a specific user."
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "slack-sdk>=3.27",
]

[tool.uv]
package = false
