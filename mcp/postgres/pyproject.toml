[project]
name = "postgres-mcp"
version = "0.1.0"
description = "MCP server exposing query tools for one or more named PostgreSQL databases."
requires-python = ">=3.11"
dependencies = [
    "mcp>=1.0.0",
    "psycopg[binary]>=3.2",
]

[tool.uv]
package = false
